version: 2
jobs:
  build:
    docker:
      - image: circleci/php:latest
    branches:
      only:
        - DEV
    steps:
      - run : ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no  "cd /app/testdeploy/ && bash ./deploy.sh"
